#------------------------------------------------------------------------------
# ENVIRONMENT STATE
#------------------------------------------------------------------------------
# development, production, test
NODE_ENV=development

#------------------------------------------------------------------------------
# SERVER (BACKEND)
#------------------------------------------------------------------------------
# PORT
PORT=3000

#------------------------------------------------------------------------------
# CLIENT (FRONTEND)
#------------------------------------------------------------------------------
# CLIENT URL
CLIENT_URL=http://localhost:5000

#------------------------------------------------------------------------------
# POSTGRESQL (PRIMARY DB)
#------------------------------------------------------------------------------
PG_HOST=localhost
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=admin
PG_DATABASE=lms


DATABASE_URL=postgres://${PG_USER}:${PG_PASSWORD}@${PG_HOST}:${PG_PORT}/${PG_DATABASE}

#------------------------------------------------------------------------------
# MONGODB (REVIEWS/QUIZZES)
#------------------------------------------------------------------------------
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=
MONGO_PASSWORD=
MONGO_DATABASE=lms

# If auth is not enabled, leave MONGO_USER/MONGO_PASSWORD empty
MONGODB_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DATABASE}
